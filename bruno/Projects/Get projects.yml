info:
  name: Get projects
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/projects"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
