{
  "name": "codebox",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "dev:client": "bun run --filter @codebox/client dev",
    "dev:server": "bun run --filter @codebox/server dev",
    "build:client": "bun run --filter @codebox/client build",
    "build:server": "bun run --filter @codebox/server build"
  },
  "dependencies": {
    "zod": "^4.3.6"
  }
}
